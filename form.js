document.addEventListener('DOMContentLoaded',()=>{let f=document.getElementById('feedbackForm');if(f)f.onsubmit=e=>{e.preventDefault();localStorage.setItem('feedback',JSON.stringify({name:name.value,email:email.value,message:message.value}));alert('Submission saved');f.reset();};});